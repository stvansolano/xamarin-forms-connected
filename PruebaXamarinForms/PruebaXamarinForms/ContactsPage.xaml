<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PruebaXamarinForms.ContactsPage"
             x:Name="RootPage">
  <StackLayout Orientation="Vertical"
               BindingContext="{Binding Source={x:Reference RootPage}}">
    <ListView ItemsSource="{Binding Items}"
              IsRefreshing="{Binding IsBusy}"
              IsPullToRefreshEnabled="True"
              RefreshCommand="{Binding RefreshCommand}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding Name}"
                    Detail="{Binding Version}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <StackLayout Orientation="Horizontal">
      <Button x:Name="loginButton" Text="Sign-in" MinimumHeightRequest="30" Command="{Binding LoginCommand}" />  
      <Button x:Name="logoutButton" Text="Logout" MinimumHeightRequest="30" Command="{Binding LogoutCommand}" IsEnabled="False" />  
    </StackLayout>
    
    <ActivityIndicator IsRunning="{Binding IsBusy}" />
  </StackLayout>
</ContentPage>